function titleCase(str) {
  return str.toLowerCase().replace(/\b(\w)/g, (s) => s.toUpperCase());
}

console.log([titleCase("hi bala!")]);
console.log([titleCase("how are you?")]);
//output:
["Hi bala!"]["How Are You?"];
